# AI Engineering Learning Log

## Week 1 Summary: Python Foundations & Professional Workflow

### Day 1 & 2: Environment & Git Setup
- Configured VS Code, Python 3.12+, and Git.
- Learned standard 3-step Git workflow: `git add .`, `git commit -m ""`, `git push`[cite: 3].
- Configured `.gitignore` to keep `__pycache__` out of GitHub repository.

### Day 3 & 4: Data Structures & Clean Code
- **Lists vs Tuples:** Lists are mutable; Tuples are immutable.
- **Dictionaries & Sets:** Key-value mappings and unique value storage.
- **Functions:** Applied type hinting, `*args` (tuples), and `**kwargs` (dictionaries).

### Day 5: OOP Fundamentals (`oop_demo.py`)
- Modeled `BankAccount` system using Classes, `__init__` constructor, instance variables (`self`), and methods.

### Day 6: File Handling & Exception Management
- Used `with open()` context manager for safe file read/write operations.
- Implemented `try-except-finally` blocks to handle `FileNotFoundError` without app crashes.